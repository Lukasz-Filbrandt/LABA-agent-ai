import DashboardPage from "@/app/dashboard/page";

// Dashboard jest stroną startową (patrz W4_DASHBOARD.md, sekcja 5 — routing).
// Ten sam komponent jest też dostępny pod /dashboard.
export default function Home() {
  return <DashboardPage />;
}
